apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx
spec:
   containers:
    - name: c1
      image: nginx
      env:
        {{- range $key,$value := .Values.env }}
        {{ $key }}: {{ $value | quote }}
        {{- end }}
