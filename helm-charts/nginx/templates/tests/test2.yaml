apiVersion: v1
kind: Pod
metadata:
  name: test-pod2
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-weight": "2"
spec:
  restartPolicy: Never
  containers:
    - name: c1
      image: alpine
      command: ["sh","-c","sleep 10 && exit 0"]