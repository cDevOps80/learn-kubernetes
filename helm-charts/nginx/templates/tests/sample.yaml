apiVersion: v1
kind: Pod
metadata:
  name: a
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: c1
      image: alpine
      command: ["sh","-c","sleep 10 && exit 0"]