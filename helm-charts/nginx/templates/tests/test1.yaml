apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-failed
    "chaitu": {{ .Template.Name }}
spec:
  containers:
    - name:
      image: alpine
      command: ["sh","-c","sleep 10 && exit 1"]
  restartPolicy: Never

