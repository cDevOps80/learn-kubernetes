apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  tolerations:
    - operator: Exists
      key: node-role.kubernetes.io/control-plane
      effect: NoSchedule
  containers:
    - name: alpine
      image: alpine
      command: ["sleep","10000"]